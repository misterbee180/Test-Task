<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testtask">

    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Task_Display"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Task_Task" />
        <activity android:name=".Task_Session" />
        <activity android:name=".TimeKeeper" />
        <activity
            android:name=".Session_Viewer"
            android:label="@string/title_activity_session_viewer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Task_Viewer"
            android:label="@string/title_activity_task_viewer"></activity>
    </application>

</manifest>